import React, {Component} from 'react';
import {connect} from 'react-redux';

export default connect()(class ProjectDetails extends Component{
        render() {
            return (
              <div>
                  IT Works*****
              </div>
            );
        }
    })

